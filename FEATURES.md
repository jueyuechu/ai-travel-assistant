# 前端功能说明 — 行程卡片 & 流式体验

> 本文解释两个核心前端概念,并附后续实现任务规划。技术实现细节见代码注释与 [TROUBLESHOOTING.md](TROUBLESHOOTING.md)。

---

## 一、行程卡片

### 是什么

行程规划从「一大段 markdown 文字」变成「结构化卡片」的可视化展示。

**之前**:后端让 LLM 把行程转成 markdown 文本,前端当普通聊天消息渲染——行程挤在文字里,没有视觉层次。

**现在**:后端直接返回结构化 JSON,前端渲染成卡片——每天一张,清晰展示天气、景点、时间、花费。

### 长什么样

```
北京                       3 天 · 预算约 ¥3000
⚠ 故宫需提前 7 天预约门票

┌ Day 1 · 2026-10-01 · 晴 20~30℃       约 ¥800 ┐
│  09:00  🏛 天安门广场              ¥0      │
│  12:00  🍜 王府井 午餐             ¥80     │
│  14:00  🏛 故宫(需预约)            ¥60     │
└─────────────────────────────────────────────┘

┌ Day 2 · …                                  ┐
│  …                                         │
└────────────────────────────────────────────┘

出行建议
· 早晚凉,带薄外套
· 故宫务必提前预约
```

### 技术实现

| 环节 | 说明 |
|---|---|
| 后端 | `POST /api/plan` 返回 `PlanResponse{ complete, missing, itinerary, advice }` |
| 前端路由 | 关键词(规划/行程/攻略/路线/安排/玩几天/几天)判断 → 走 `/api/plan`,否则走 `/chat` 流式 |
| 渲染 | `ItineraryCard.vue`:目的地头 + 提醒条 + 每天一张卡(天气/时间/类型图标/景点/花费/备注)+ 出行建议 |
| 多轮追问 | 缺字段时 `complete=false`,返回追问文本,前端继续追问累积 |

### 为什么不继续用 markdown

- markdown 是「流式文本」的载体,适合问答/天气表格;
- 行程是「结构化数据」(每天 / 每景点 / 花费),卡片能更好呈现层次、支持后续地图打点。

---

## 二、流式体验

### 是什么

对话输出的视觉反馈,让「等待」和「生成」都有反馈,不显得卡顿生硬。

### 三个状态(按时间顺序)

```
[等待]  · · ·                 ← 三点思考动画(还没收到内容)
[输出]  北京天气今天…▍          ← 逐字 + 末尾闪烁光标
[暂停]  北京天气今天…▍          ← 后端在调工具/生成,光标继续闪 = 还在生成
[完成]  北京天气今天晴转多云      ← 光标消失
```

### 为什么需要

后端 LLM 输出**不是匀速的**——生成/调工具期间会暂停(SSE 没数据),然后突然吐一大段。直接渲染就是「卡一下 → 一大片」。前端用三件套抹平这个突发:

1. **思考动画**:收到第一个字前显示三点,表示「正在想」;
2. **打字机**:逐字推进显示,把「突发的一大片」拆成连续的逐字;积压多时自动加速,不拖慢;
3. **闪烁光标**:缓冲耗尽(后端暂停)时光标继续闪,表示「还在生成」,不生硬。

### 技术实现

| 状态 | 实现 |
|---|---|
| 思考动画 | `thinking` 状态,收到第一个 token 前显示三点跳动(`think-blink`) |
| 打字机 | `requestAnimationFrame` 每帧推进游标 `displayLen`,步长按积压动态:`>200 字/帧跳 10`、`>60 跳 4`、`>15 跳 2`、否则 1 |
| 闪烁光标 | 消息带 `streaming` 标记,流未结束时 `ChatMessage` 渲染 `.typing-cursor`(`cursor-blink`) |
| 收尾 | 流结束取消打字机,立即显示完整内容,隐藏光标 |

> 注意:行程卡片是结构化输出,不适合逐字流式,用「正在规划行程…」动画兜底等待期即可。

---

## 三、接下来的实现任务

> 状态:本轮改动已编译通过。按优先级排列,① 是继续前必做的验证。

| 优先级 | 任务 | 目标 | 改动范围 |
|---|---|---|---|
| **P0** | 验证本轮改动 | 行程卡片 / RAG fallback / 流式体验 跑通 | 前端刷新即可 |
| **P1** | 地图点位 | 行程卡片里加高德地图,景点可视化打点 | 后端加 JS API key + 前端引入高德 SDK |
| **P1** | 注入当前日期 | system prompt 注入今天日期,修「明天被当今天」 | 后端 system prompt 加日期 |
| **P2** | 汇率工具 | `open.er-api.com`(免 key)补全实况查询 | 后端新增 `@Tool` + 挂到 realtimeChatClient |
| **P2** | RAG 知识库扩充 | 现在只有 6 条种子,采集真实攻略 | 数据采集 + `SeedDataInitializer` |
| **P3** | 生产化 | 鉴权 / 限流 / 缓存 / 观测;ChatMemory 换 Redis | 后端基建 |

**建议顺序**:P0 验证 → P1 地图点位(体验提升最大)→ P1 注入日期(小改动)→ P2 汇率/知识库 → P3 生产化。
